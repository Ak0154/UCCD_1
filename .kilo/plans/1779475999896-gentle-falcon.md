# Plan: Remove Duplicate Footer + Beautify ResolutionFlow

## 1. Remove Duplicate Footer

**Problem:** Two identical scrolling logo marquees exist on the page:
- `<PoweredByStrip />` inside `<main>` (last section before footer)
- `<footer>` element in `App.tsx` (the proper semantic site footer)

**Action:**
- Remove `PoweredByStrip` import from `App.tsx:13`
- Remove `<PoweredByStrip />` from JSX at `App.tsx:42`
- Delete `src/components/uccd/PoweredByStrip.tsx`

**Also clean up dead code files** (not imported anywhere):

| File | Status |
|---|---|
| `src/components/Resolution.tsx` | Dead (also has extra footer) |
| `src/components/Hero.tsx` | Dead |
| `src/components/Nav.tsx` | Dead |
| `src/components/AgentGrid.tsx` | Dead |
| `src/components/MultiChannel.tsx` | Dead |
| `src/components/DepartmentRouting.tsx` | Dead |
| `src/components/PhoneMockup.tsx` | Dead |
| `src/components/ZigzagArrow.tsx` | Dead |

## 2. Beautify ResolutionFlow

**File:** `src/components/uccd/ResolutionFlow.tsx`

### Stage Timeline Bar (the 01-04 step indicators)
- Add custom SVG icons for each of the 4 stages:
  - 01 Channel intake → inbox/message icon
  - 02 Normalize → document/transform icon  
  - 03 Agent orchestrator → brain/sparkle icon
  - 04 Route + resolve → check/arrow-target icon
- Animated glow pulse ring on the active stage circle
- Completed stages get a subtle checkmark
- Widen the progress line with a gradient (accent to subtle) and animated shimmer

### Data Display Card (the animated payload card)
- Per-stage contextual styling:
  - Stage 1 (Channel intake): message-bubble feel
  - Stage 2 (Normalize): data-envelope feel with badge chips for the envelope fields
  - Stage 3 (Agent orchestrator): grid of agent cards with status dots (ready/processing)
  - Stage 4 (Route + resolve): SLA countdown timer visual, team badge
- Add small contextual SVG icons next to each data row label
- Smoother enter/exit animations with staggered row reveals
- Subtle gradient border instead of solid `border-accent/30`

### Simulation Controls Header
- Scenario selector: show scenario name + channel badge instead of just `SIM-XXX`
- Add subtle background pattern or gradient to the main card wrapper
- Better visual hierarchy between the header bar and the content area

### Section Heading ("How UCCD resolves a complaint")
- Add animated subtle background shapes (blurred accent circles) behind the heading area
- Slightly enhanced typography with letter-spacing refinement
- Add a decorative line or gradient divider below heading

### Overall Polish
- Subtle animated particle effect in the background of the section
- Smoother framer-motion spring transitions for the payload card
- Ensure responsive behavior is maintained

---

## Implementation Order
1. Delete dead code files + PoweredByStrip
2. Update App.tsx
3. Rewrite ResolutionFlow.tsx with all enhancements
4. Verify no build errors