# Fix Review Issues — Uncommitted Changes

## Summary
Fix 1 critical syntax error, 2 warnings, and 2 suggestions found during code review of uncommitted changes.

---

## 1. [CRITICAL] `agents/escalation_agent.py:43-52` — Invalid Python syntax in `_compute_breach_probability_ml`

**Problem:** Keyword arguments inside a list literal (`priority_tier=3`, `sla_tier_hours=sla_hours`, etc.) cause a `SyntaxError` at import time.

**Fix:** Replace the malformed list construction with a plain positional list-of-lists matching expected model feature order:
```python
features = [[
    severity_score,
    3,                      # priority_tier
    sla_hours,              # sla_tier_hours
    queue_size,
    1 if regulatory_flag else 0,
    1 if vip_customer else 0,
    1,                      # channel_is_digital
    datetime.datetime.now().hour,
]]
```

---

## 2. [WARNING] `agents/escalation_agent.py:103` — Unsafe `state['raw_text']` access

**Problem:** `state['raw_text'][:200]` on line 103 will raise `KeyError` if `raw_text` is absent from the state dict, crashing the pipeline uncaught.

**Fix:** Use `state.get("raw_text", "")[:200]` to safely default to empty string.

---

## 3. [WARNING] `services/regulatory_service.py:9` — Tight coupling via `from services.sla_service import r`

**Problem:** Both services share the same Redis handle imported from `sla_service`, creating bidirectional coupling.

**Fix:** Extract the Redis connection into a new shared module `services/cache.py`:
- Create `services/cache.py` with the `redis.Redis` connection setup
- Update `services/sla_service.py` to import from `services.cache`
- Update `services/regulatory_service.py` to import from `services.cache`

---

## 4. [SUGGESTION] `frontend/src/pages/LoginPage.tsx:19` — Default password in UI

**Problem:** Default password `'password'` pre-populates the field.

**Fix:** Change `useState('password')` to `useState('')`.

---

## 5. [SUGGESTION] `frontend/src/pages/SignInPage.tsx:268` — Default password in UI

**Problem:** Default password `'Test@123'` pre-populates the field.

**Fix:** Change `useState('Test@123')` to `useState('')`.

---

## Implementation Steps

1. Fix `agents/escalation_agent.py` (issues #1 and #2 — same file)
2. Create `services/cache.py` and refactor Redis imports (issue #3)
3. Fix `frontend/src/pages/LoginPage.tsx` (issue #4)
4. Fix `frontend/src/pages/SignInPage.tsx` (issue #5)
5. Verify no import errors: `python -c "from agents.escalation_agent import run_escalation"`
6. Verify Redis refactor: `python -c "from services.sla_service import r; from services.regulatory_service import r"`
7. Verify frontend builds: `npm run build` (or `npm run dev` for dev check)